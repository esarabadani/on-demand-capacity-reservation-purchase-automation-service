# Python packages the Function App uses at runtime.
# Azure Functions will `pip install -r requirements.txt` during deployment.

# Azure Functions Python worker + programming model v2 decorators.
azure-functions>=1.20.0

# Auth: picks up managed identity in Azure, developer creds locally.
azure-identity>=1.17.1

# Compute management SDK - lets us CRUD Capacity Reservation Groups and
# Capacity Reservations. Used in cr_manager.py.
azure-mgmt-compute>=33.0.0

# Table Storage client - used in cr_state.py for crState / crAttempts.
azure-data-tables>=12.5.0

# azure-core provides the base exception types (ResourceNotFoundError etc.).
# It's a transitive dep of the others but we pin a minimum for clarity.
azure-core>=1.30.0
